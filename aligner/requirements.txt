# FastAPI service runtime
fastapi==0.115.4
uvicorn[standard]==0.32.0
python-multipart==0.0.12
pydantic==2.9.2

# Audio IO (needed for any actual decode work; pure-Python fallback for mp3/wav headers)
soundfile==0.12.1
numpy==1.26.4

# ASR with word-level timestamps
faster-whisper==1.0.3
# Note: faster-whisper needs a HuggingFace model on first run (e.g. Systran/faster-whisper-base).
# Set ALIGN_WHISPER_MODEL to choose size; smaller = faster, less accurate.

# Vocal separation (optional, pluggable). Demucs is heavy (torch ~2GB).
# Install only if you actually need source separation; otherwise set ALIGN_DEMUCS_CMD=""
demucs==4.0.1
# torch / torchaudio — left out of requirements.txt on purpose (huge, platform-specific).
# Install with: pip install torch torchaudio

# Chinese text normalization & fuzzy matching
pypinyin==0.53.0
rapidfuzz==3.10.1
python-Levenshtein==0.26.1